

<%= paginate @pages %><br>
<% @pages.each do |page| %>
  <%=page.post %>---

  <%=page.comment %> <%=link_to "Edit", edit_page_path(page.id)%> <%=link_to "Delete", page_path(page.id),method: :delete, data: {confirm: "Are you sure you want to delete?"}%><br>

<%end%><br>

<%=link_to "Create New Post", new_page_path%>
